<div class="container-fluid">
  <h3 class="grey-text text-darken-2 center">Dashboard</h3>
  <div class="row">
    <div class="col s12 m3 center">
      <div class="z-depth-5 card blue-grey darken-1">
        <div class="card-image">
          <img src="{{ user.image }}" class="circle responsive-img" alt="" />
        </div>
        <div class="card-action">
          <span class="card-title white-text flow-text"
            ><%= locals.user.firstName %> <%= locals.user.lastName %></span
          >
        </div>
      </div>
    </div>
    <div class="col s12 m5 center">
      <!-- <div class="white darken-3 input-field col s12">
        <select class="dashboard-month-selector">
          <option value="" disabled selected>Change Month</option>
          {{#each monthList}}
          <option value="{{ value }}">{{ text }}</option>
          {{else}}
          <option value="" disabled selected>Year ends</option>
          {{/each}}
        </select>
      </div> -->
      <!-- <br /><br /><br /><br />
      <div class="z-depth-5 card blue-grey">
        <div class="card-content">
          <div class="card-title white-text">Leaves</div>
          <table class="striped">
            <thead>
              <tr>
                <th>Date</th>
                <th>Lunch</th>
                <th>Dinner</th>
              </tr>
            </thead>
            <tbody>
              {{#each leaveList}}
              <tr>
                <td>{{ date }}</td>
                <td>{{ lunch }}</td>
                <td>{{ dinner }}</td>
              </tr>
              {{else}}
              <tr>
                <td colspan="3" class="center">No Leave applied yet</td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
      </div> -->
    </div> 
      <div class="col s12 m4 center">
        <div class="z-depth-5 card blue-grey darken-1">
          <div class="card-content white-text">
            <span class="card-title"
              ><h5>Amount information for <%= month.text %></h5></span
            >
            <!-- {{!--
          <h5 class="flow-text">
            Total amount for Breakfast : {{ totalLunchPrice }}
          </h5>
          <h5 class="flow-text">
            Total amount for Dinner : {{ totalDinnerPrice }}
          </h5>
          --}} {{!--
          <h5 class="flow-text">Previous Balance : {{ prevBalance }}</h5>
          --}} -->
          </div>
          <div class="card-action">
            <h5 class="white-text flow-text">Total : <%= totalAmount %></h5>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
